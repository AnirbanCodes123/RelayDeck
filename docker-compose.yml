# CPU-only. On NVIDIA hosts:
# docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d --build
services:
  mediamtx:
    image: bluenviron/mediamtx:latest
    command: ["/mediamtx.yml"]
    init: true
    restart: unless-stopped
    stop_grace_period: 15s
    ports:
      - "8554:8554"
      - "9997:9997"
    volumes:
      - ./mediamtx.yml:/mediamtx.yml:ro

  portal:
    build: .
    restart: unless-stopped
    stop_grace_period: 20s
    depends_on:
      mediamtx:
        condition: service_started
        restart: true
    environment:
      MEDIAMTX_HOST: mediamtx
      MEDIAMTX_RTSP_PORT: "8554"
      RTSP_PUBLIC_HOST: localhost
      DATA_DIR: /app/data
      MAX_STREAMS: "80"
      EDGE_AGENT_PORT: "9000"
      HOST_PROC: /host/proc
    pid: "host"
    cap_add:
      - SYS_PTRACE
      - SYS_ADMIN
    security_opt:
      - apparmor:unconfined
    ports:
      - "9009:9009"
    volumes:
      - relaydeck-data:/app/data
      - /proc:/host/proc:ro
    ulimits:
      nofile:
        soft: 65536
        hard: 65536

volumes:
  relaydeck-data:
